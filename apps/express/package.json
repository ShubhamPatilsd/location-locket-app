{
  "name": "@memoir/express",
  "private": true,
  "version": "0.1.0",
  "main": "./src/app.ts",
  "scripts": {
    "start": "ts-node build/app.js",
    "build": "tsc",
    "dev": "pnpm with-env ts-node-dev src/app.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf .turbo node_modules",
    "lint": "eslint .",
    "lint:fix": "pnpm lint --fix",
    "type-check": "tsc --noEmit",
    "with-env": "dotenv -e ../../.env --"
  },
  "devDependencies": {
    "@memoir/api": "*",
    "@memoir/webhook": "*",
    "@types/cors": "^2.8.16",
    "@types/express": "^4.17.21",
    "@types/node": "^20.9.1",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0",
    "dotenv-cli": "^4.0.0",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.1",
    "express": "^4.18.2",
    "zod": "^3.21.4"
  }
}
